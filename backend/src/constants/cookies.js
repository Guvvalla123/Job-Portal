/**
 * Cookie names for auth + CSRF (keep in sync with frontend cookie readers if any).
 */
const REFRESH_TOKEN_COOKIE = "jid_refresh";
const CSRF_COOKIE = "jid_csrf";

module.exports = { REFRESH_TOKEN_COOKIE, CSRF_COOKIE };
